/node_modules
/vendor
/build
/languages/nfd-site-migrator.php

# Generated by `yarn generate:css` before every webpack build.
/src/styles/nfd-site-migrator.css

cypress.env.json
cypress/videos
cypress/screenshots
.idea

# PHPUnit: the result cache, and a local override of phpunit.xml.dist.
.phpunit.result.cache
phpunit.xml
dist/

# Playwright: the browser suite's own output. The WordPress it drives lives in the system
# temp directory, not here.
/tests/playwright/test-results
/test-results
/tests/playwright/screenshots
/playwright-report
/.wp-env.override.json
